import 'package:ai_chat_bot/core/core.dart';

class IepBrushTypePicker extends StatelessWidget {
  const IepBrushTypePicker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      children: [
        Icon(
          Icons.circle_rounded,
        ),
      ],
    );
  }
}
