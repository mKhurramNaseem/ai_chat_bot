enum ImageEditPageType { simpleType, drawingType, shapeDrawingType }

enum StrokeType { thin, medium, thick }

enum ShapeType {
  line,
  oval,
  rectangle,
  rRectangle,
  pentagon,
  hexagon,

  isocelesTriangle,
  customLine,
}
