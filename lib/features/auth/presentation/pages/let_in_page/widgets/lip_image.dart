import 'package:ai_chat_bot/core/core.dart';

class LipImage extends StatelessWidget {
  const LipImage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Image.asset(AppImages.simpleImage));
  }
}
